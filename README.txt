Description of folders:
-----------------------

1. `scripts` folder: contains all the python scripts for loading and processing the dataset. Also has a sub-folder named `output` which contains the output files of the processed data.



Description of script files (under `script` folder):
----------------------------------------------------

1. loadSampledata.py: loads the files (movie files) under the dataset, calculates their average ratings and stores them in avgRatingsOut.txt output file


